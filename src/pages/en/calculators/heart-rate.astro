---
import BaseLayout from '@/layouts/BaseLayout.astro';
import HeartRateCalculator from '@/components/calculators/HeartRateCalculator.astro';
import { t } from '@/utils/i18n';

const title = t('heartRate.title', 'en') + ' - ' + t('site.title', 'en');
const description = t('heartRate.metaDescription', 'en');
---

<BaseLayout title={title} description={description} lang="en">
  <div class="container">
    <div class="page-header">
      <h1>{t('heartRate.title', 'en')}</h1>
      <p class="page-description">{t('heartRate.description', 'en')}</p>
    </div>

    <HeartRateCalculator lang="en" />
  </div>

  <style>
    .page-header {
      text-align: center;
      padding: 2rem 0;
    }

    .page-header h1 {
      font-size: 2.5rem;
      color: #333;
      margin-bottom: 0.5rem;
    }

    .page-description {
      font-size: 1.25rem;
      color: #666;
    }
  </style>
</BaseLayout>
