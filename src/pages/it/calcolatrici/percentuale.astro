---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PercentageCalculator from '@/components/calculators/PercentageCalculator.astro';
import { t } from '@/utils/i18n';

const title = t('percentage.title', 'it') + ' - ' + t('site.title', 'it');
const description = t('percentage.metaDescription', 'it');
---

<BaseLayout title={title} description={description} lang="it">
  <div class="container">
    <div class="page-header">
      <h1>{t('percentage.title', 'it')}</h1>
      <p class="page-description">{t('percentage.description', 'it')}</p>
    </div>

    <PercentageCalculator lang="it" />
  </div>

  <style>
    .page-header {
      text-align: center;
      padding: 2rem 0;
    }

    .page-header h1 {
      font-size: 2.5rem;
      color: #333;
      margin-bottom: 0.5rem;
    }

    .page-description {
      font-size: 1.25rem;
      color: #666;
    }
  </style>
</BaseLayout>
