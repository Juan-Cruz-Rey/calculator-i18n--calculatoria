---
import BaseLayout from '@/layouts/BaseLayout.astro';
import ProteinCalculator from '@/components/calculators/ProteinCalculator.astro';
import { t } from '@/utils/i18n';

const title = t('protein.title', 'sv') + ' - ' + t('site.title', 'sv');
const description = t('protein.metaDescription', 'sv');
---

<BaseLayout title={title} description={description} lang="sv" calculator="protein">
  <div class="container">
    <div class="page-header">
      <h1>{t('protein.title', 'sv')}</h1>
      <p class="page-description">{t('protein.description', 'sv')}</p>
    </div>

    <ProteinCalculator lang="sv" />
  </div>
</BaseLayout>

<style>
  .page-header {
    text-align: center;
    padding: 2rem 0 1rem;
  }

  .page-header h1 {
    font-size: 2.5rem;
    color: #333;
    margin-bottom: 0.5rem;
  }

  .page-description {
    font-size: 1.25rem;
    color: #666;
  }
</style>
