---
import BaseLayout from '@/layouts/BaseLayout.astro';
import PaceCalculator from '@/components/calculators/PaceCalculator.astro';
import { t } from '@/utils/i18n';

const title = t('pace.title', 'sv') + ' - ' + t('site.title', 'sv');
const description = t('pace.metaDescription', 'sv');
---

<BaseLayout title={title} description={description} lang="sv" calculator="pace">
  <div class="container">
    <div class="page-header">
      <h1>{t('pace.title', 'sv')}</h1>
      <p class="page-description">{t('pace.description', 'sv')}</p>
    </div>

    <PaceCalculator lang="sv" />
  </div>
</BaseLayout>

<style>
  .page-header {
    text-align: center;
    padding: 2rem 0 1rem;
  }

  .page-header h1 {
    font-size: 2.5rem;
    color: #333;
    margin-bottom: 0.5rem;
  }

  .page-description {
    font-size: 1.25rem;
    color: #666;
  }
</style>
