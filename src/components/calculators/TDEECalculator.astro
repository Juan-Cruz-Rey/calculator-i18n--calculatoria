---
import { t, type Locale } from '@/utils/i18n';

const { lang } = Astro.props as { lang: Locale };
---

<div class="tdee-calculator">
  <div class="calculator-form">
    <h2>{t('tdee.form.title', lang)}</h2>

    <form id="tdee-form">
      <!-- Unit System Toggle -->
      <div class="form-group unit-toggle">
        <label>
          <input type="radio" name="unitSystem" value="metric" checked />
          {t('units.metric', lang)}
        </label>
        <label>
          <input type="radio" name="unitSystem" value="imperial" />
          {t('units.imperial', lang)}
        </label>
      </div>

      <!-- Age -->
      <div class="form-group">
        <label for="age">{t('tdee.form.ageLabel', lang)}</label>
        <input
          type="number"
          id="age"
          name="age"
          min="15"
          max="80"
          required
          placeholder={t('tdee.form.agePlaceholder', lang)}
        />
        <span class="unit">{t('common.years', lang)}</span>
      </div>

      <!-- Gender -->
      <div class="form-group">
        <label for="gender">{t('tdee.form.genderLabel', lang)}</label>
        <select id="gender" name="gender" required>
          <option value="male">{t('common.male', lang)}</option>
          <option value="female">{t('common.female', lang)}</option>
        </select>
      </div>

      <!-- Height (Metric) -->
      <div class="form-group height-metric">
        <label for="height">{t('tdee.form.heightLabel', lang)}</label>
        <input
          type="number"
          id="height"
          name="height"
          min="100"
          max="250"
          step="0.1"
          required
          placeholder={t('tdee.form.heightPlaceholder', lang)}
        />
        <span class="unit">{t('units.cm', lang)}</span>
      </div>

      <!-- Height (Imperial) -->
      <div class="form-group height-imperial" style="display: none;">
        <label for="heightFt">{t('tdee.form.heightLabel', lang)}</label>
        <div class="input-group">
          <input
            type="number"
            id="heightFt"
            name="heightFt"
            min="3"
            max="8"
            placeholder="Feet"
          />
          <span class="unit">{t('units.ft', lang)}</span>
          <input
            type="number"
            id="heightIn"
            name="heightIn"
            min="0"
            max="11"
            placeholder="Inches"
          />
          <span class="unit">{t('units.in', lang)}</span>
        </div>
      </div>

      <!-- Weight -->
      <div class="form-group">
        <label for="weight">{t('tdee.form.weightLabel', lang)}</label>
        <input
          type="number"
          id="weight"
          name="weight"
          min="30"
          max="300"
          step="0.1"
          required
          placeholder={t('tdee.form.weightPlaceholder', lang)}
        />
        <span class="unit weight-unit">{t('units.kg', lang)}</span>
      </div>

      <!-- Activity Level -->
      <div class="form-group">
        <label for="activityLevel">{t('tdee.form.activityLabel', lang)}</label>
        <select id="activityLevel" name="activityLevel" required>
          <option value="sedentary">{t('tdee.activity.sedentary', lang)}</option>
          <option value="light">{t('tdee.activity.light', lang)}</option>
          <option value="moderate">{t('tdee.activity.moderate', lang)}</option>
          <option value="active">{t('tdee.activity.active', lang)}</option>
          <option value="very-active">{t('tdee.activity.veryActive', lang)}</option>
          <option value="extra-active">{t('tdee.activity.extraActive', lang)}</option>
        </select>
      </div>

      <!-- Body Fat Percentage (Optional) -->
      <div class="form-group">
        <label for="bodyFat">{t('tdee.form.bodyFatLabel', lang)} ({t('common.optional', lang)})</label>
        <input
          type="number"
          id="bodyFat"
          name="bodyFat"
          min="5"
          max="50"
          step="0.1"
          placeholder={t('tdee.form.bodyFatPlaceholder', lang)}
        />
        <span class="unit">%</span>
      </div>

      <!-- Buttons -->
      <div class="form-actions">
        <button type="submit" class="btn-primary">{t('common.calculate', lang)}</button>
        <button type="reset" class="btn-secondary">{t('common.reset', lang)}</button>
      </div>
    </form>
  </div>

  <div class="calculator-results" id="results" style="display: none;" role="region" aria-live="polite" aria-atomic="true">
    <h2>{t('tdee.results.title', lang)}</h2>

    <div class="result-card tdee">
      <h3>{t('tdee.results.tdee', lang)}</h3>
      <p class="value" id="tdee-value">-</p>
      <p class="subtitle">{t('tdee.results.tdeeDesc', lang)}</p>
    </div>

    <div class="goals-section">
      <h3>{t('tdee.results.goalsTitle', lang)}</h3>

      <div class="goal-category">
        <h4>{t('tdee.results.weightLoss', lang)}</h4>
        <div class="goal-grid">
          <div class="goal-item">
            <span class="goal-label">{t('tdee.results.mild', lang)} (0.25 kg/{t('common.week', lang)})</span>
            <span class="goal-value" id="mild-loss">-</span>
          </div>
          <div class="goal-item">
            <span class="goal-label">{t('tdee.results.moderate', lang)} (0.5 kg/{t('common.week', lang)})</span>
            <span class="goal-value" id="weight-loss">-</span>
          </div>
          <div class="goal-item">
            <span class="goal-label">{t('tdee.results.extreme', lang)} (1 kg/{t('common.week', lang)})</span>
            <span class="goal-value" id="extreme-loss">-</span>
          </div>
        </div>
      </div>

      <div class="goal-category">
        <h4>{t('tdee.results.weightGain', lang)}</h4>
        <div class="goal-grid">
          <div class="goal-item">
            <span class="goal-label">{t('tdee.results.mild', lang)} (0.25 kg/{t('common.week', lang)})</span>
            <span class="goal-value" id="mild-gain">-</span>
          </div>
          <div class="goal-item">
            <span class="goal-label">{t('tdee.results.moderate', lang)} (0.5 kg/{t('common.week', lang)})</span>
            <span class="goal-value" id="weight-gain">-</span>
          </div>
          <div class="goal-item">
            <span class="goal-label">{t('tdee.results.extreme', lang)} (1 kg/{t('common.week', lang)})</span>
            <span class="goal-value" id="extreme-gain">-</span>
          </div>
        </div>
      </div>
    </div>

    <div class="macros-section">
      <h3>{t('tdee.results.macrosTitle', lang)}</h3>
      <div class="macros-grid">
        <div class="macro-item protein">
          <div class="macro-icon">P</div>
          <div class="macro-details">
            <strong>{t('tdee.results.protein', lang)}</strong>
            <span id="protein-grams">-</span> g
            <span class="macro-calories">(<span id="protein-cal">-</span> cal)</span>
          </div>
        </div>
        <div class="macro-item carbs">
          <div class="macro-icon">C</div>
          <div class="macro-details">
            <strong>{t('tdee.results.carbs', lang)}</strong>
            <span id="carbs-grams">-</span> g
            <span class="macro-calories">(<span id="carbs-cal">-</span> cal)</span>
          </div>
        </div>
        <div class="macro-item fats">
          <div class="macro-icon">F</div>
          <div class="macro-details">
            <strong>{t('tdee.results.fats', lang)}</strong>
            <span id="fats-grams">-</span> g
            <span class="macro-calories">(<span id="fats-cal">-</span> cal)</span>
          </div>
        </div>
      </div>
      <p class="macros-note">{t('tdee.results.macrosNote', lang)}</p>
    </div>

    <div class="bmr-info">
      <strong>{t('tdee.results.bmr', lang)}:</strong>
      <span id="bmr-value">-</span>
      <p class="bmr-desc">{t('tdee.results.bmrDesc', lang)}</p>
    </div>
  </div>

  <div class="calculator-info">
    <h2>{t('tdee.info.title', lang)}</h2>
    <p>{t('tdee.info.description', lang)}</p>

    <h3>{t('tdee.info.formulasTitle', lang)}</h3>
    <div class="formula-box">
      <strong>Mifflin-St Jeor:</strong>
      <p class="formula">{t('tdee.info.mifflinFormula', lang)}</p>
    </div>

    <h3>{t('tdee.info.activityTitle', lang)}</h3>
    <ul class="activity-list">
      <li><strong>{t('tdee.activity.sedentary', lang)}</strong> - {t('tdee.info.sedentaryDesc', lang)}</li>
      <li><strong>{t('tdee.activity.light', lang)}</strong> - {t('tdee.info.lightDesc', lang)}</li>
      <li><strong>{t('tdee.activity.moderate', lang)}</strong> - {t('tdee.info.moderateDesc', lang)}</li>
      <li><strong>{t('tdee.activity.active', lang)}</strong> - {t('tdee.info.activeDesc', lang)}</li>
      <li><strong>{t('tdee.activity.veryActive', lang)}</strong> - {t('tdee.info.veryActiveDesc', lang)}</li>
      <li><strong>{t('tdee.activity.extraActive', lang)}</strong> - {t('tdee.info.extraActiveDesc', lang)}</li>
    </ul>

    <p class="limitations">{t('tdee.info.limitations', lang)}</p>
  </div>
</div>

<style>
  .tdee-calculator {
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem;
  }

  .calculator-form, .calculator-results, .calculator-info {
    background: white;
    border-radius: 8px;
    padding: 2rem;
    margin-bottom: 2rem;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  }

  h2 {
    margin-top: 0;
    color: #333;
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
  }

  h3 {
    color: #333;
    font-size: 1.25rem;
    margin-bottom: 1rem;
  }

  h4 {
    color: #555;
    font-size: 1.1rem;
    margin-bottom: 0.75rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 500;
    color: #555;
  }

  .form-group input[type="number"],
  .form-group select {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid #ddd;
    border-radius: 4px;
    font-size: 1rem;
  }

  .form-group input[type="number"]:focus,
  .form-group select:focus {
    border-color: #E53935;
  }

  .unit {
    display: inline-block;
    margin-left: 0.5rem;
    color: #888;
    font-size: 0.9rem;
  }

  .unit-toggle {
    display: flex;
    gap: 1rem;
    padding: 1rem;
    background: #f5f5f5;
    border-radius: 4px;
  }

  .unit-toggle label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;
    margin: 0;
  }

  .input-group {
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  .input-group input {
    flex: 1;
  }

  .form-actions {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
  }

  .btn-primary, .btn-secondary {
    flex: 1;
    padding: 0.875rem;
    border: none;
    border-radius: 4px;
    font-size: 1rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
  }

  .btn-primary {
    background: linear-gradient(135deg, #E53935 0%, #C62828 100%);
    color: white;
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, #C62828 0%, #B71C1C 100%);
  }

  .btn-secondary {
    background: #f5f5f5;
    color: #333;
  }

  .btn-secondary:hover {
    background: #e0e0e0;
  }

  .result-card {
    text-align: center;
    padding: 2rem;
    background: linear-gradient(135deg, #EF5350 0%, #E53935 100%);
    color: white;
    border-radius: 8px;
    margin-bottom: 2rem;
  }

  .result-card h3 {
    margin: 0 0 1rem 0;
    font-size: 1.2rem;
    color: white;
  }

  .result-card .value {
    font-size: 3rem;
    font-weight: bold;
    margin: 0;
  }

  .result-card .subtitle {
    font-size: 1rem;
    margin: 0.5rem 0 0 0;
    opacity: 0.9;
  }

  .goals-section {
    margin: 2rem 0;
  }

  .goal-category {
    margin-bottom: 2rem;
  }

  .goal-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1rem;
  }

  .goal-item {
    padding: 1rem;
    background: #f9f9f9;
    border-radius: 4px;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .goal-label {
    color: #666;
    font-size: 0.9rem;
  }

  .goal-value {
    color: #333;
    font-size: 1.5rem;
    font-weight: 600;
  }

  .macros-section {
    margin: 2rem 0;
    padding: 1.5rem;
    background: #f8f9fa;
    border-radius: 8px;
  }

  .macros-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .macro-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    background: white;
    border-radius: 8px;
    border-left: 4px solid;
  }

  .macro-item.protein {
    border-color: #E53935;
  }

  .macro-item.carbs {
    border-color: #43A047;
  }

  .macro-item.fats {
    border-color: #FB8C00;
  }

  .macro-icon {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 1.5rem;
    color: white;
  }

  .macro-item.protein .macro-icon {
    background: #E53935;
  }

  .macro-item.carbs .macro-icon {
    background: #43A047;
  }

  .macro-item.fats .macro-icon {
    background: #FB8C00;
  }

  .macro-details {
    flex: 1;
  }

  .macro-details strong {
    display: block;
    margin-bottom: 0.25rem;
    color: #333;
  }

  .macro-details span {
    font-size: 1.2rem;
    font-weight: 600;
    color: #555;
  }

  .macro-calories {
    display: block;
    font-size: 0.85rem !important;
    font-weight: 400 !important;
    color: #888 !important;
    margin-top: 0.25rem;
  }

  .macros-note {
    font-size: 0.85rem;
    color: #666;
    font-style: italic;
    margin: 0;
  }

  .bmr-info {
    padding: 1.5rem;
    background: #e3f2fd;
    border-radius: 4px;
    border-left: 4px solid #2196F3;
  }

  .bmr-info strong {
    color: #1976D2;
    font-size: 1.1rem;
  }

  .bmr-info span {
    color: #333;
    font-size: 1.3rem;
    font-weight: 600;
    margin-left: 0.5rem;
  }

  .bmr-desc {
    margin: 0.5rem 0 0 0;
    color: #555;
    font-size: 0.9rem;
  }

  .calculator-info {
    background: #f9f9f9;
  }

  .calculator-info p {
    margin-bottom: 1rem;
    line-height: 1.6;
    color: #555;
  }

  .formula-box {
    background: white;
    padding: 1rem;
    border-left: 4px solid #E53935;
    margin: 1rem 0;
    border-radius: 4px;
  }

  .formula {
    font-family: monospace;
    margin: 0.5rem 0 0 0;
    color: #333;
  }

  .activity-list {
    margin: 1rem 0;
    padding-left: 1.5rem;
  }

  .activity-list li {
    margin-bottom: 0.75rem;
    line-height: 1.5;
    color: #555;
  }

  .limitations {
    font-size: 0.9rem;
    font-style: italic;
    margin-top: 1.5rem;
  }

  @media (max-width: 768px) {
    .goal-grid {
      grid-template-columns: 1fr;
    }

    .macros-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  import type { TDEEInput, TDEEResult, Gender, ActivityLevel } from '@/utils/calculators/tdee';
  import { calculateTDEE, poundsToKg, feetInchesToCm } from '@/utils/calculators/tdee';

  const form = document.getElementById('tdee-form') as HTMLFormElement;
  const results = document.getElementById('results') as HTMLDivElement;
  const unitSystemInputs = document.querySelectorAll('input[name="unitSystem"]');
  const heightMetric = document.querySelector('.height-metric') as HTMLDivElement;
  const heightImperial = document.querySelector('.height-imperial') as HTMLDivElement;
  const weightUnit = document.querySelector('.weight-unit') as HTMLSpanElement;

  // Handle unit system change
  unitSystemInputs.forEach(input => {
    input.addEventListener('change', (e) => {
      const target = e.target as HTMLInputElement;
      const isMetric = target.value === 'metric';

      heightMetric.style.display = isMetric ? 'block' : 'none';
      heightImperial.style.display = isMetric ? 'none' : 'block';

      const heightInput = document.getElementById('height') as HTMLInputElement;
      const heightFt = document.getElementById('heightFt') as HTMLInputElement;
      const heightIn = document.getElementById('heightIn') as HTMLInputElement;

      if (isMetric) {
        heightInput.required = true;
        heightFt.required = false;
        heightIn.required = false;
        weightUnit.textContent = 'kg';
      } else {
        heightInput.required = false;
        heightFt.required = true;
        heightIn.required = true;
        weightUnit.textContent = 'lbs';
      }
    });
  });

  // Handle form submission
  form.addEventListener('submit', (e) => {
    e.preventDefault();

    const formData = new FormData(form);
    const unitSystem = formData.get('unitSystem') as string;
    const age = parseInt(formData.get('age') as string);
    const gender = formData.get('gender') as Gender;
    const activityLevel = formData.get('activityLevel') as ActivityLevel;
    let weight = parseFloat(formData.get('weight') as string);
    let heightCm: number;
    const bodyFat = formData.get('bodyFat') as string;
    const bodyFatPercentage = bodyFat ? parseFloat(bodyFat) : undefined;

    if (unitSystem === 'metric') {
      heightCm = parseFloat(formData.get('height') as string);
    } else {
      const feet = parseInt(formData.get('heightFt') as string) || 0;
      const inches = parseInt(formData.get('heightIn') as string) || 0;
      heightCm = feetInchesToCm(feet, inches);
      weight = poundsToKg(weight);
    }

    const input: TDEEInput = {
      age,
      gender,
      heightCm,
      weightKg: weight,
      activityLevel,
      bodyFatPercentage,
    };

    try {
      const result = calculateTDEE(input);
      displayResults(result);
    } catch (error) {
      alert('Error calculating TDEE. Please check your inputs.');
      console.error(error);
    }
  });

  function displayResults(result: TDEEResult) {
    const tdeeValue = document.getElementById('tdee-value') as HTMLParagraphElement;
    const bmrValue = document.getElementById('bmr-value') as HTMLSpanElement;
    const mildLoss = document.getElementById('mild-loss') as HTMLSpanElement;
    const weightLoss = document.getElementById('weight-loss') as HTMLSpanElement;
    const extremeLoss = document.getElementById('extreme-loss') as HTMLSpanElement;
    const mildGain = document.getElementById('mild-gain') as HTMLSpanElement;
    const weightGain = document.getElementById('weight-gain') as HTMLSpanElement;
    const extremeGain = document.getElementById('extreme-gain') as HTMLSpanElement;

    const proteinGrams = document.getElementById('protein-grams') as HTMLSpanElement;
    const proteinCal = document.getElementById('protein-cal') as HTMLSpanElement;
    const carbsGrams = document.getElementById('carbs-grams') as HTMLSpanElement;
    const carbsCal = document.getElementById('carbs-cal') as HTMLSpanElement;
    const fatsGrams = document.getElementById('fats-grams') as HTMLSpanElement;
    const fatsCal = document.getElementById('fats-cal') as HTMLSpanElement;

    tdeeValue.textContent = `${result.tdee} cal/day`;
    bmrValue.textContent = `${result.bmr} cal/day`;
    mildLoss.textContent = `${result.mildWeightLoss} cal/day`;
    weightLoss.textContent = `${result.weightLoss} cal/day`;
    extremeLoss.textContent = `${result.extremeWeightLoss} cal/day`;
    mildGain.textContent = `${result.mildWeightGain} cal/day`;
    weightGain.textContent = `${result.weightGain} cal/day`;
    extremeGain.textContent = `${result.extremeWeightGain} cal/day`;

    proteinGrams.textContent = result.macros.protein.grams.toString();
    proteinCal.textContent = result.macros.protein.calories.toString();
    carbsGrams.textContent = result.macros.carbs.grams.toString();
    carbsCal.textContent = result.macros.carbs.calories.toString();
    fatsGrams.textContent = result.macros.fats.grams.toString();
    fatsCal.textContent = result.macros.fats.calories.toString();

    // Show results
    results.style.display = 'block';
    results.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }

  // Handle form reset
  form.addEventListener('reset', () => {
    results.style.display = 'none';
  });
</script>
